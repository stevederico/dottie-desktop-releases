// Local only — copy to Release.xcconfig (gitignored). Never commit Release.xcconfig.
// cp Release.xcconfig.example Release.xcconfig

DEVELOPMENT_TEAM = YOUR_TEAM_ID
PRODUCT_BUNDLE_IDENTIFIER = com.example.dottie

// For Developer ID / notarize builds, also set in Xcode or uncomment:
// CODE_SIGN_IDENTITY[sdk=macosx*] = Developer ID Application
// CODE_SIGN_STYLE = Manual
